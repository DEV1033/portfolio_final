// Fill these in with your Supabase project's values (Project Settings -> API).
// The anon key is safe to expose publicly; it only ever grants what your RLS policies allow.
window.SUPABASE_CONFIG = {
  url: "https://evgxquktmnypccpskpog.supabase.co",
  anonKey: "sb_publishable_RHraqxEn4hmevkuMnOurEA_E0116yb9",
  // The email of the single admin user you create in Supabase Auth (Authentication -> Users).
  // It is never shown in the UI - only the passkey (the account's password) is typed in.
  adminEmail: "devchaudhary1033@gmail.com",
};
